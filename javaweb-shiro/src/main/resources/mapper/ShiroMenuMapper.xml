<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.javaweb.shiro.mapper.ShiroMenuMapper">

    <!-- 获取人员菜单权限列表 -->
    <select id="getMenuListByAdminId" resultType="com.javaweb.shiro.entity.Menu">
        select * from sys_menu where id in (
                select menu_id from sys_role_menu where role_id in (
                    select id from sys_admin_role where admin_id = #{adminId} and mark = 1
                ) and mark = 1
        ) and mark = 1 order by sort asc;
    </select>

</mapper>
